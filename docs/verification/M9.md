# M9 verification — Corpus Delivery, Profile, and Real Verification

Date: 2026-08-20

Status values below describe only work actually executed in this checkout.

## Requirement/evidence table

| Requirement | Evidence actually run | Result | Status |
| --- | --- | --- | --- |
| Balanced curated corpus | Streamed the complete official `lichess_db_puzzle.csv.zst`; `scripts/buildPuzzleSubset.js` scanned 4,804,341 eligible rows | 7,200 puzzles; each of six seedable categories has 1,200 puzzles (600 short, 600 long), ratings 800–2200 | PASS |
| Artifact under 5 MB | Generated `puzzles-subset.jsonl.gz` | 383,839 bytes | PASS |
| Manifest and SHA-256 | Independent `Get-FileHash` plus generated manifest | `0c3be26539d64355de521908d884f4f48bc21d1eda46769f932d45910753cf6e` | PASS |
| GitHub Release hosting | Created `m9-corpus-v1`, uploaded asset, downloaded it back, and re-hashed | Downloaded size and SHA-256 exactly match local artifact | PASS |
| User-initiated first launch | Real entry HTML and wiring tests inspect the modal/button; app starts import only from the explicit button | No silent download path | PASS |
| Checksum before import | `test/corpusBootstrap.test.js` with a corrupted expected digest | Rejected before any puzzle insert | PASS |
| No network / dropped network | Stubbed fetch rejection and a stream error after partial bytes | Both leave zero puzzle rows and remain retryable | PASS |
| Interrupted import / app-kill atomicity | Real SQLite transaction with a forced failure after the first puzzle insert | Rollback leaves zero rows; SQLite itself owns crash rollback for an uncommitted transaction | PASS |
| Successful re-launch | Import same manifest twice | Second launch skips fetch when version and populated table match | PASS |
| Fresh zero-history session | Imported artifact into real SQLite, then used `MobileSqlitePuzzleLibrary` and `TrainingOrchestrator` | Tactical focus starts and exactly two sessions are queued | PASS |
| Profile empty state | Real profile renderer in `test/appWiring.test.js` and `test/profile.test.js` | Intentional no-sessions, no-weakness, and progress-locked copy | PASS |
| Profile populated state | Seeded real SQLite aggregates and rendered profile | Correct totals, weakness bars, recent result/move count, and corpus status | PASS |
| SQLite settings round-trip | Wrote settings, then read through a new async connection wrapper | Display name, chess.com username, avatar, theme, and engine level persist | PASS |
| Engine difficulty applied | Constructed a targeted session at skill 17 | New `PracticeSession.skillLevel` is 17 | PASS |
| Bottom navigation and 44 px targets | Real entry HTML/CSS plus dev-only browser preview | Practice/Profile tabs present; controls have 44–48 px minimum heights | PASS |
| Phone and unfolded Fold visual layout | Dev-only browser preview built outside `www/`; headless Chrome screenshots captured for phone and unfolded layouts | Profile two-column unfolded layout verified; phone render inspected. Browser preview is not native-storage evidence | PARTIAL |
| Illegal move, check/pin, castling, en-passant, promotion | Full Node suite includes chess.js legality, castling, en-passant, promotion, and illegal-UCI rejection | Logic remains green; manual browser gestures were not available in the in-app browser runtime | PARTIAL |
| Release web bundle | `npm run cap:sync` (which runs `npm run build:web`) | Capacitor copied the current bundle; SQLite plugin registered | PASS |
| Android build | Gradle 8.14.3 `assembleDebug` | Build successful; package `com.chessanalyst.app`, target SDK 36 | PASS |
| APK content freshness | Opened the built APK and inspected `assets/public/bundle.js` | Contains M9 release URL, profile renderer, and async puzzle fix | PASS |
| Physical Fold 3 install and launch | `adb devices -l` | No Android device was connected | PENDING |
| Native corpus download/import | Requires connected phone and real WebView/SQLite bridge | Not run | PENDING |
| Native persistence across kill/relaunch | Requires connected phone | Not run | PENDING |
| Stockfish WASM under `capacitor://` and 15-second response | Requires connected phone/WebView logs | Not run | PENDING |
| Battery/performance during session | Requires connected phone and a real session | Not run | PENDING |

## Executed verification levels

- Level 1: full Node suite PASS (final count recorded in the build handoff).
- Level 2: real-entry HTML, corpus gate, empty/populated profile, settings round-trip, and real-SQLite corpus/session wiring PASS.
- Level 3: dev-only browser preview PARTIAL. It exercises the real profile renderer and CSS but deliberately does not claim `@capacitor-community/sqlite` browser support or native persistence.
- Level 4: Capacitor sync and APK compilation PASS; physical device checks PENDING because `adb` reported no connected devices.

## Browser storage limitation

The browser preview uses a clearly marked dev-only adapter generated under the
ignored `artifacts/browser-preview` directory. It is never loaded by
`www/index.html` and was generated only after the release APK sync. Its
browser-local setting persistence is layout/interactivity evidence only and
does not prove the native SQLite plugin works.

## APK evidence

- File: `Chess-Analyst-Orange-Cat-M9-debug.apk`
- Size: 19,140,901 bytes
- SHA-256: `B5D0608971E6C1B4BF6A2B0532FAFA68C0E7DBEF22631CD1308EFB05F077EC6D`
- Package: `com.chessanalyst.app`
- Label: `Chess Analyst`
- Version: `1.0` (`versionCode=1`)
- Compile/target SDK: 36

## Remaining device handoff

Connect and authorize the Galaxy Z Fold3, then install this exact APK. Capture
WebView console output while downloading/importing the corpus, start a fresh
session, make one legal move, verify exactly one engine reply within 15 seconds,
kill/relaunch the app, and confirm corpus/settings/history persistence. Until
those actions are run, every device-only row above remains PENDING.
