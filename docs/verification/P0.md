# P0 verification report

Verified on 2026-08-15 with Node 24.19.0, npm 11.9.0,
`chess.js@1.4.0`, and real `node:sqlite`.

## Requirement/evidence table

| Requirement | Test/evidence | Expected | Actual | Status |
| --- | --- | --- | --- | --- |
| SQLite puzzle corpus uses bounded streaming import | 500,000-row, 50 MiB generated Lichess-shape CSV imported through `importPuzzleCsv()` with progress memory sampling | Exact row count; no full-corpus JS object array | 500,000 puzzles and 1,000,000 theme rows in 8.096 s; RSS 58.3 → 112.7 MiB | PASS |
| Theme and ply queries are indexed and unique | SQLite row counts, `COUNT(DISTINCT ...)`, and `EXPLAIN QUERY PLAN` | No duplicate puzzle rows; indexed theme/ply access | 166,667 distinct `fork` matches; theme primary-key and puzzle primary-key indexes used; `idx_puzzles_step_count` exists | PASS |
| Failed corpus import is atomic | Existing-corpus fixture, then valid row followed by invalid row with `batchSize=1` | Previous corpus remains; no partial replacement | Existing puzzle and its one theme row remained; attempted rows absent | PASS |
| Buckets are short 2–6 and long 8–12 | `test/themeMapping.test.js` | Exact configured ranges | `[2,6]` and `[8,12]` observed | PASS |
| Start-slow normal path returns two seeds | Real SQLite targeting integration and normal bucket test | One short plus one long | IDs `short`, `long`; count 2 | PASS |
| Missing long bucket falls back safely | Long-fallback regression test | Longest matching puzzle; `bucketDowngraded=true` | Six-ply `f2` returned with flag true | PASS |
| Move rows retain analysis-grade fields | Practice-session plus SQLite semantic round-trip | Before/after eval, best move, PV, mate flag, FEN, move, response, timestamp | All fields matched the session summary after readback | PASS |
| Eval delta uses mover perspective | Permanent +80/+20 sign-flip truth test | `-100` cp | `-100` cp | PASS |
| Mate sentinels are not normal cp deltas | Mate parsing and delta tests | Mate flagged; delta null | `isMateScore=true`; `computeEvalDelta()` returned null | PASS |
| Legacy eval migration is truthful | Old-schema SQLite fixture migrated and inspected | `eval_cp_before=35`; no fabricated after value | `eval_cp_before=35`, `eval_cp_after=NULL`, mate flag 0 | PASS |
| practical_time remains tracked but unseeded | Real SQLite ranking `practical_time → tactical` | Advice for first; tactical two-seed target | Advice recorded; tactical selected with two SQLite puzzles | PASS |
| Motif-ready FEN applies Moves[0] legally | Starting-position fixture with `Moves[0]=e2e4` | Start FEN is post-`e2e4` and legal | Exact canonical post-`e2e4` FEN observed | PASS |
| Remaining solution is not forced | Exported continuation `e7e5`; user instead played legal `c7c5` | Free Stockfish game proceeds | `c7c5` accepted and logged; engine replied `g1f3` | PASS |
| Bad setup/dependency failure leaves state valid | Missing/illegal setup tests and worker-failure turn test | Explicit failure; no partial turn mutation | Exceptions observed; current FEN unchanged and logs remained empty | PASS |

## Regression suite

```text
npm ci --cache /tmp/chess-npm-cache
npm test --cache /tmp/chess-npm-cache
```

Result: 31 tests, 31 passed, 0 failed.

- Level A (unit): 20/20
- Level B (integration with real chess.js and/or SQLite): 11/11
- Level C (real Stockfish end-to-end): 0/0, not a P0 dependency; required by M1

## Claims not made

- The complete public Lichess corpus was not imported in this environment.
  Scale evidence is the executed 500,000-row import above.
- Real Stockfish WASM was not exercised by P0. That is the M1 gate.
