# M6 verification — data-driven theming

Date: 2026-08-15

## Status

PARTIAL. The data architecture, validation, fallback, renderer token adapter,
piece map, and chess.com CSS generator pass. A real Electron window is still
blocked by M5's missing display server, so visual application in the packaged
desktop surface remains UNVERIFIED. Final artwork is an explicit later user
input. M6 is NOT READY.

## Requirement/evidence table

| Requirement | Test/evidence | Expected | Actual | Status |
| --- | --- | --- | --- | --- |
| Swappable animal packs | Registry test | Cat and panda are separate packs | IDs were exactly `cat`, `panda` | PASS |
| Data/assets/tokens only | Dependency audit | No training/game/import/storage logic | Theme module had no imports from core, engine, analysis, import, or storage | PASS |
| Board palette | Token/application test | Light/dark/frame tokens swap | Panda applied `--board-dark=#587460` and all 11 variables | PASS |
| Piece graphics mapping | Pack validation + renderer audit | All 12 pieces are theme data | Both packs contained 12 mappings; renderer reads `activeTheme.pieces` | PASS |
| UI chrome | CSS-variable adapter | Background, panel, accent, text, border swap | All chrome variables generated from selected pack | PASS |
| chess.com visual skin | CSS generator test | Selected palette becomes CSS only | Panda background/board colors emitted; no JS/FEN/Stockfish/analysis content | PASS |
| CSS token safety | Injection fixture | Reject arbitrary CSS payloads | Non-hex injected accent threw and pack fell back to cat | PASS |
| Missing theme handling | Fallback tests | Missing/broken theme cannot break app | Both resolved to validated cat without throwing | PASS |
| Theme independent of functionality | Existing controller/core regression | Theme selection does not alter chess state | Core/controller APIs contain no theme dependency; all regressions green | PASS |
| Runtime theme IPC | IPC allowlist test | Named local-renderer-only operation | `theme:set` registered and remote sender remained rejected | PASS |
| Actual native UI theme switch | Electron BrowserWindow interaction | Cat↔panda visibly updates app | No display server | UNVERIFIED |
| Actual chess.com theme resilience | Remote Electron window | CSS applies/fails soft against current site | No display server/network window | UNVERIFIED |
| Regression safety | `node --test` | Entire suite passes | 75 passed, 0 failed, 0 skipped | PASS |

## Test levels

- Level A — unit: 48/48 passed.
- Level B — integration: 25/25 passed.
- Level C — end-to-end: 2/2 passed (real Stockfish paths retained). Electron visual E2E is unverified.

## Real dependencies

- Electron binary: YES; visual window NO.
- Stockfish: YES (regression).
- SQLite: YES (regression).
- Claude: NO (M3 gate).
- Ollama: NO (M3 gate).

## Failure cases

- missing theme ID;
- structurally incomplete theme;
- CSS-injection-shaped token;
- chess.com CSS insertion/load failure remains caught outside chess logic.

## User-input dependency

The cat and panda palette/chrome architecture is complete. Piece artwork is
intentionally represented by validated Unicode placeholder maps. Final custom
cat/panda artwork and preferences require user assets/direction later; replacing
the maps does not touch game, engine, analysis, import, or storage code.

## Merge recommendation

NOT READY while M5's real desktop window gate remains unresolved. After that
gate, verify the visible cat/panda switch and chess.com fail-soft skin, then
independent review can mark this ready. Do not auto-merge.
